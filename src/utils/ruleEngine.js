// Simple rule engine stub
// Returns a tag based on the profession category. For now, returns "default".
export function applyRules(category) {
  // In a real implementation, you'd have complex logic here.
  // For demonstration, we just return a static tag.
  return "default";
}
